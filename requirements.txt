accelerate==1.13.0
av==13.1.0
diffusers==0.35.0
numpy==2.2.6
pillow==11.3.0
PyYAML==6.0.3
safetensors==0.4.5
sentencepiece==0.2.1
tqdm==4.67.1
transformers==4.56.0
# flash-attn and torch are installed separately by setup_venv.sh -- NOT
# listed here. torch: unpinned from cu132 (this GB300 box's CUDA version),
# not the original torch==2.9.1 pin, which `pip install -r requirements.txt`
# would otherwise reinstall and clobber. flash-attn: needs
# --no-build-isolation (its setup.py imports torch at build time) and
# building from source on this architecture, so it's installed as its own
# step after torch is already in place, not bundled with everything else.

